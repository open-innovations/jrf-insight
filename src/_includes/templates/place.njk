---
layout: ./page.njk
tags:
  - place
---

<p>
This is a mockup place page for {{ name }} ({{ key }}).
</p>


{{ comp.nav_block({
  place_key: key,
  parents: parents,
  children: place.children | values | flatten,
  dir: {
    spotlight: true
  }
}) | safe }}

{% set geojson_key = key %}

<pre>
{{place_data[key].council_tax_support.rows | dump(2) }}
</pre>

{{ key }}
{% if geojson[key] %}
{# {{ comp.oi.map.zoomable({
        config: {
          data: place_data[key].council_tax_support.rows,
          geojson: 
          {
            data: geojson[key],
            key: place.geojson.join_key
          },
          columns: [{
            name: "label",
            template: "{{geography_code}}: {{council_tax_support | toLocaleString()}}"
          }],
          key: "geography_code",
          value: "council_tax_support",
          scale: "Viridis",
          min: place_data[key].council_tax_support.rows | min,
          max: place_data[key].council_tax_support.rows | max,
          tooltip: "label",
          legend:
          {
            position: "top right",
            items: [{
                value: 600000,
                label: 'test'},
                {
                value: 500000,
                label: '500k'},
                {
                value: 400000,
                label: '400k'
            }]
          }
        }
    }
  ) | safe }} #}
{% endif %}