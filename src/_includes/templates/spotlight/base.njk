---
layout: ../page.njk
tags:
  - spotlight
---
{% set place_page = search.page("place key=" + geography) %}

{{ comp.nav_block({
  dir: {
    place: true
  },
  place_key: geography,
  parents: place_page.parents,
  children: place_page.place.children | values | flatten,
  place_prefix: '/spotlight/' + spotlight + '/'
}) | safe }}

{{ content | safe }}
