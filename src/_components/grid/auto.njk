---
css: |
  .grid-auto {
    display: grid;
    gap: var(--auto-grid-gap, 1rem);
    grid-template-columns: repeat( auto-fit, minmax(250px, 1fr) );
  }
---

<div class="grid-auto" style="{{ style }}">
  {{ content | safe }}
</div>