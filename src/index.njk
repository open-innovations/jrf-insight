---
title: Home
key: E12999901
---
<p class="welcome">
  Welcome to the Northern England Insight Finder (NEIF).
</p>

{% set about %}{% include 'partials/about.njk' %}{% endset %}
{% comp 'collapse', { heading: 'About this prototype', level: 2 } %}
  {{ about | safe }}
{% endcomp %}

{% comp 'grid.masonry', { style: "--masonry-brick-size:12em;" } %}
  {% comp 'link.block_link', { href: "/spotlight/", style: "--normal-color:white;--normal-background:var(--jrf-strong-pink);" } %}
  {% comp 'grid.block', { style: "text-align:center;align-items:center;" } %}
    <div class="big-number">{{ search.pages("spotlight main") | length }} spotlights</div>
    <p>shedding light on a poverty-related topic</p>
  {% endcomp %}
  {% endcomp %}

  {% comp 'link.block_link', { href: "/place/", style: "--normal-color:white;--normal-background:var(--jrf-purple);" } %}
  {% comp 'grid.block', { style: "text-align:center;align-items:center;" } %}
    <div class="big-number">{{ places.list | length }} places</div>
    <p>profiling key information about a place</p>
  {% endcomp %}
  {% endcomp %}

  {% comp 'link.block_link', { href: "/metadata/", style: "--normal-color:white;--normal-background:var(--jrf-moderate-cyan);" } %}
  {% comp 'grid.block', { style: "text-align:center;" } %}
    <div class="big-number">{{ metadata.catalogue | length }} datasets</div>
    <p> cataloguing the sources of the data shown on the site</p>
  {% endcomp %}
  {% endcomp %}
{% endcomp %}

{# 
<p>
  Below there's an example*, based on child poverty data, showing how data can be navigated,
  the outputs and visualisations you'll be able to interact with and download.
</p>

<p>
  * Here we would need a simple visualisation of going from national to local and where child poverty
  feeds into the different spotlight - we thought this could be a simple visualisation drawn in PowerPoint
  or something similar - doesn't need to be anything fancy.
</p>
#}

<h2>Search bar</h2>
{% include 'partials/search.njk' %}



{% if false %}

<h2>General headlines and top-level region visualisations</h2>

<p>
Maybe drawn from <a href="https://www.jrf.org.uk/report/uk-poverty-2023">JRF UK Poverty report</a>.
</p>
{{ comp.oi.map.zoomable({
        config: {
          data: query.getPlaceData(place[key].relations.children),
          geojson: 
          {
            key: "ons_code",
            data: place[key].map
          },
          columns: [{
            name: "label",
            template: "{{geography_code}}: {{Number of persons | toLocaleString()}}"
          }],
          key: "geography_code",
          value: "Number of persons",
          scale: "Viridis",
          min: 10000,
          max: 7000000,
          tooltip: "label",
          legend:
          {
            position: "top right",
            items: [{
                value: 7000000,
                label: '7M'},
              {
                value: 10000,
                label: '10k'
            }]
          }
        }
    }
  ) | safe }}

{% endif %}